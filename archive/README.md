# Archived research material

**Historical material only. The current manuscript is [v1.35 at the repository root](../README.md).**

- [`v1.24_snapshot/`](v1.24_snapshot/) contains the older reproduction files and embedded manuscript copies that previously occupied the repository root. It preserves their relative directory layout and byte contents. Some files inside this cumulative snapshot belong to still earlier revisions; the folder name describes the imported snapshot, not every individual file's version.
- [`provenance/`](provenance/) holds superseded snapshot provenance.
- [`relocation_manifest.json`](relocation_manifest.json) records every moved path and its unchanged Git blob hash.

These files are not the current manuscript or current validation results. Historical scripts may require their original environment and should be run from the archived snapshot directory where applicable.

The complete later cumulative reproduction ZIP is still pending. This archive does not claim to fill that gap. Earlier committed manuscript versions also remain available in Git history.

## Single-live-version policy

Validated updates replace `fixed_space_prime_action_v1.tex`, `fixed_space_prime_action_v1.pdf`, `RH_G1_G2_research_log.md`, and `v1_revision_notes.md` at the repository root. Keep current supporting evidence and provenance clearly linked from the root README. Place superseded snapshots and provenance here; never present them as a second live version. Preserve existing commit history and unrelated changes.
