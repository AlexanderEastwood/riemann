# NS-38 — archival disclosure

Evidence-availability correction, not recovery or a certificate.

Both original NS-1 groups are explicitly NOT ARCHIVED (one v1.31 report survives). Recovery remains open. See the versioned HTML report and machine-readable claim changes. Five scientific files each in v1.35/v1.36/v1.37 match their legacy hashes. No numerical gates replayed; actual build 246 pages, zero undefined/duplicate references. No version bump for this disclosure alone.

The v2 HTML preserves v1 and records the independent review fixes: local front-matter disclosures, explicit historical verification headings, and the unavailable block-metric closure marked blocked on the map. Printed analytic proofs unchanged; build remains 246 pages and zero undefined/duplicate references.

The v3 report corrects a v1/v2 inventory error: the v1.35 source, revision notes and research log are all archived byte-for-byte at tag v1.35 and match their legacy hashes. The current-tree verifier misses historical locations; these three files are not missing originals. See v135_publication_tag_provenance.json.
