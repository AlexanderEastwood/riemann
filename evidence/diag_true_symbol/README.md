Diagnostic (2026-09-21, Claude); not a certificate. See results.md.
Correct version of the retracted "line symbol" experiment: the manuscript's β_a
validated against the assembled matrix to 2e-7, its negative set, and the
ground-state level split at λ=3. Run from the repo root with .venv/bin/python;
beta_true.py runs the validation when executed directly (~5 min).
