Diagnostic (2026-09-21, Claude); not a certificate. See results.md.
Correct version of the retracted "line symbol" experiment: the manuscript's β_a
validated against the assembled matrix to 2e-7, its negative set, the
ground-state level split at λ=3 and λ=4, the ground's lattice-cell
distribution across λ (selfsim.py), and the lobe of β_a at the first zeta
ordinate against the ground's mass there (nearzero.py). Run from the repo root
with .venv/bin/python; beta_true.py runs the validation when executed directly
(~5 min). cert.py holds the Arb-to-mpmath midpoint conversion the scripts share.
