# Pre-claim candidate screen

Proposal: On the complete compact smooth core, multiply every off-diagonal
correlation in the original Weil form by exp(-epsilon*abs(t)), retaining
its archimedean diagonal and both poles. First take epsilon=2. Try the
one-sided inequality q_epsilon[f]-q[f] <= C||f||^2 for every support window,
with a single finite C. Together with a nonnegative damped form this would
supply the WEIL-FLOOR target. Inspect epsilon_a ->0 as a possible change of
hypothesis only if it retains a proved nonnegative comparison and the same
uniform remainder; do not claim those inputs.

Shared-input group: WEIL-FLOOR, frozen. Closest NS47 ordinary-closable
comparison, NS50 critical localization, v136 uniform floor. The exact
hypothesis match to NS47 must be checked before any research claim.

Arithmetic input beyond the functional equation: exact Lambda(n) and
-Lambda Dirichlet sum = zeta'/zeta in Re(s)>1, grouped with the original
Gamma(s/2), s(s-1) and pi factors. The proposed new estimate is the
uniform one-sided cost of removing damping. No such estimate is given.
Positive Euler coefficients alone are not proposed to imply RH.

Control screen before proof/certification: evaluate the actual damped
logarithmic-derivative expression 2 Re(F'/F)(epsilon+i*t) at epsilon=2 and
five recorded ordinates, at30 and50 decimal digits. Use controls/screen for
Davenport-Heilbronn; compare its negative interior point and the matching
original-zeta value. Include the NS100 and NS101 function controls, and a
shifted zeta-product control with nonnegative Euler coefficients. These are
function-expression checks, not a full match of Riemann's gamma/pole data
or the unquantified all-test remainder bound. Label that full bound
inconclusive on controls, and mark exact-original-arithmetic transfer
not-applicable. No sampled result proves a universal inequality.

Additional arithmetic check: compare the epsilon=2 symbol computed from
finite actual prime-power sums at N=1000,4000 with the completed logarithmic
derivative; record the elementary all-integer log(n)/n^(5/2) tail allowance.
This is a normalization and domain check, not a certificate or a new bound.
No new full Weil window calculation or archive-ground scan.

Wall check before the screen: Same open gap, with a potential Known wall
from NS47. What changes: a specified exponential regularizer and the exact
prime/gamma/pole identity; no new cofinal estimate yet.

What success changes: only admit a research row if the proposed inequality
escapes the recorded obstruction and has an independent arithmetic input.
What failure changes: reject before claim and archive the overlap/domain
audit without a theorem node, manuscript edit or version.
Budget: one bounded control/identity screen, one audit PR at most.
Disposition before claim: DEFER pending screen and NS47 hypothesis match.
No research row has been claimed; this is a pre-registration admission test.
